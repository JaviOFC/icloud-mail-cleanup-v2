---
phase: 03-report-review-safe-execution
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified: []
autonomous: false
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "Full workflow runs end-to-end: report -> review -> execute (dry-run)"
    - "Report displays tier summary, cluster details, and confidence distributions"
    - "Review session can be quit and resumed mid-flow"
    - "Dry-run execution shows correct delete plan without side effects"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 3 workflow end-to-end on real data: report generation, interactive review, and dry-run execution.

Purpose: This checkpoint confirms the full user-facing flow works correctly against the real Envelope Index and Phase 2 checkpoint data before the user attempts actual deletions. It validates report rendering, auto-triage accuracy, review UX, and dry-run execution output.

Output: User confirmation that the workflow is functional and ready for real use.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-report-review-safe-execution/03-CONTEXT.md
@.planning/phases/03-report-review-safe-execution/03-01-SUMMARY.md
@.planning/phases/03-report-review-safe-execution/03-02-SUMMARY.md
@.planning/phases/03-report-review-safe-execution/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end Phase 3 workflow verification</name>
  <files></files>
  <action>
    Present the user with verification steps for the complete Phase 3 workflow. Run the full test suite to confirm all tests pass before presenting verification instructions.
  </action>
  <verify>
    <automated>cd /path/to/project && uv run pytest tests/ -v</automated>
  </verify>
  <done>User confirms all workflow steps function correctly on real data</done>
  <what-built>
    Complete Phase 3 workflow: report generation (terminal + JSON + Markdown), interactive review with auto-triage and propagation, and safe execution with dry-run mode and action logging.
  </what-built>
  <how-to-verify>
    1. **Report generation:**
       ```bash
       cd /path/to/project
       uv run python -m icloud_cleanup report --format all --output /tmp/cleanup-report
       ```
       - Verify terminal output shows tier-first summary with counts, storage, confidence bars
       - Check `/tmp/cleanup-report/cleanup_report.json` has full tier/cluster data
       - Check `/tmp/cleanup-report/cleanup_report.md` is readable markdown with tables

    2. **Auto-triage + review (first few clusters only):**
       ```bash
       uv run python -m icloud_cleanup review
       ```
       - Verify auto-triage summary appears (e.g., "Auto-resolved X emails across Y clusters")
       - Review 2-3 clusters using different actions (approve one, skip one, inspect one)
       - Press Ctrl-C to quit mid-review

    3. **Session resume:**
       ```bash
       uv run python -m icloud_cleanup review --resume
       ```
       - Verify it picks up where you left off (skips already-decided clusters)

    4. **Dry-run execution:**
       ```bash
       uv run python -m icloud_cleanup execute
       ```
       - Verify it shows what WOULD be deleted without executing
       - Verify action log created at ~/.icloud-cleanup/action_log.db
       - Verify output says "Run with --execute to carry out deletions"

    5. **Full test suite:**
       ```bash
       uv run pytest tests/ -v
       ```
       - All tests pass including new Phase 3 tests
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
User confirms all 5 verification steps pass. Full test suite green. Workflow is functional end-to-end.
</verification>

<success_criteria>
- Report renders correctly in all three formats with real data
- Auto-triage meaningfully reduces the Review tier
- Interactive review flows smoothly with questionary prompts
- Session resume works after Ctrl-C quit
- Dry-run execution produces correct output without side effects
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-report-review-safe-execution/03-04-SUMMARY.md`
</output>
