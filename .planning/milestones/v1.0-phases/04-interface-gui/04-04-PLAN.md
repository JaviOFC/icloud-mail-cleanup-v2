---
phase: 04-interface-gui
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/icloud_cleanup/tui/screens/help_overlay.py
  - src/icloud_cleanup/tui/screens/review.py
  - src/icloud_cleanup/tui/__init__.py
  - src/icloud_cleanup/tui/app.tcss
  - tests/test_tui.py
autonomous: false
requirements:
  - TUI-10
  - TUI-09

must_haves:
  truths:
    - "User can press ? to open a keyboard shortcut reference overlay"
    - "Help overlay shows all keybindings organized by screen"
    - "Dark and light themes are toggled with T key and look correct"
    - "All 4 screens are functional and navigable end-to-end"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: "src/icloud_cleanup/tui/screens/help_overlay.py"
      provides: "HelpScreen (ModalScreen) showing keybinding reference"
      exports: ["HelpScreen"]
  key_links:
    - from: "src/icloud_cleanup/tui/__init__.py"
      to: "src/icloud_cleanup/tui/screens/help_overlay.py"
      via: "? binding pushes HelpScreen modal"
      pattern: "push_screen.*help"
    - from: "src/icloud_cleanup/tui/app.tcss"
      to: "all screens"
      via: "Global tier color classes and theme variables"
      pattern: "tier-trash|tier-keep"
---

<objective>
Add the help overlay (? key), polish theme toggle styling, and run a final human-verified end-to-end checkpoint of the complete TUI.

Purpose: Final polish and integration verification -- ensures all screens work together, themes look correct, and the keyboard-driven experience is discoverable.
Output: Complete, polished TUI with help system and visual verification.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-interface-gui/04-CONTEXT.md
@.planning/phases/04-interface-gui/04-RESEARCH.md
@.planning/phases/04-interface-gui/04-01-SUMMARY.md
@.planning/phases/04-interface-gui/04-02-SUMMARY.md
@.planning/phases/04-interface-gui/04-03-SUMMARY.md

<interfaces>
<!-- Screens created in prior plans -->
From tui/__init__.py:
```python
class CleanupApp(App):
    MODES = {
        "dashboard": "DashboardScreen",
        "review": "ReviewScreen",
        "execute": "ExecuteScreen",
        "pipeline": "PipelineScreen",
    }
    BINDINGS = [
        Binding("d", "switch_mode('dashboard')", "Dashboard", priority=True),
        Binding("r", "switch_mode('review')", "Review", priority=True),
        Binding("e", "switch_mode('execute')", "Execute", priority=True),
        Binding("p", "switch_mode('pipeline')", "Pipeline", priority=True),
        Binding("question_mark", "push_screen('help')", "Help"),
        Binding("t", "toggle_dark", "Theme"),
        Binding("q", "quit", "Quit"),
    ]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create help overlay and polish theme styling</name>
  <files>
    src/icloud_cleanup/tui/screens/help_overlay.py,
    src/icloud_cleanup/tui/__init__.py,
    src/icloud_cleanup/tui/app.tcss,
    tests/test_tui.py
  </files>
  <action>
    1. Create `src/icloud_cleanup/tui/screens/help_overlay.py` -- `HelpScreen(ModalScreen)`:
       - BINDINGS: escape=dismiss, ?=dismiss (both close the overlay)
       - `compose()`: Vertical container with:
         - Static title: "Keyboard Shortcuts" (bold, centered)
         - Rich Table (rendered via Static widget) organized by section:

         **Global:**
         | Key | Action |
         | D | Dashboard |
         | R | Review |
         | E | Execute |
         | P | Pipeline |
         | T | Toggle theme |
         | ? | This help |
         | Q | Quit |

         **Review Screen:**
         | Key | Action |
         | Up/Down | Navigate clusters |
         | Space | Toggle selection |
         | A | Approve selected |
         | S | Skip selected |
         | I | Toggle inspect (show emails) |

         **Execute Screen:**
         | Key | Action |
         | Enter | Start execution |
         | C | Cancel execution |

         **Pipeline Screen:**
         | Key | Action |
         | Enter | Start pipeline |
         | C | Cancel pipeline |

       - Style: semi-transparent backdrop, centered panel with padding, max-width 60, max-height 80%

    2. Update `src/icloud_cleanup/tui/__init__.py`:
       - Register HelpScreen: `SCREENS = {"help": HelpScreen}` (use install_screen or SCREENS dict depending on Textual 8 API)
       - Verify the `question_mark` binding correctly triggers `push_screen('help')`

    3. Polish `src/icloud_cleanup/tui/app.tcss`:
       - Ensure tier color classes work in both dark and light themes
       - HelpScreen modal styling: `HelpScreen { align: center middle; }`, inner container with background, border, padding
       - Verify all screen-level TCSS files have sensible defaults for both themes
       - Test that `$error`, `$success`, `$accent`, `$warning` CSS variables resolve to appropriate colors in both themes (they should -- Textual handles this natively, but verify the tier class mappings)

    4. Add tests to `tests/test_tui.py`:
       - `test_help_overlay_opens()`: Press ?, verify HelpScreen is pushed (check `app.screen.__class__.__name__`)
       - `test_help_overlay_closes()`: Open help, press Escape, verify back to previous screen
       - `test_theme_toggle_both_directions()`: Toggle theme, verify it changes, toggle again, verify it changes back

    5. Run the full test suite to catch any regressions from all 4 plans:
       `python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /path/to/project && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ? key opens help overlay showing all keybindings by section
    - Escape or ? closes the help overlay
    - Theme toggle works in both directions (dark->light->dark)
    - Tier colors render correctly in both themes
    - Full test suite passes (all existing + TUI tests)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end TUI visual and functional verification</name>
  <action>Human verifies the complete TUI application visually and functionally across all 4 screens.</action>
  <what-built>
    Complete Textual TUI application with 4 screens (Dashboard, Review, Execute, Pipeline), keyboard navigation, theme toggle, and help overlay. The TUI reads classification data from checkpoint, provides an interactive review workflow with bulk actions and propagation, and can execute approved deletions or re-run the classification pipeline.
  </what-built>
  <how-to-verify>
    1. Launch the TUI:
       ```
       cd /path/to/project
       python -m icloud_cleanup tui
       ```
       (Requires a checkpoint at ~/.icloud-cleanup/checkpoint.jsonl from prior runs)

    2. **Dashboard (D key):**
       - Verify tier summary table shows 4 tiers with counts, sizes, sparklines
       - Verify "Potential savings" banner shows storage number prominently
       - Verify colors match expectations (red=Trash, green=Active, blue=Historical, yellow=Review)

    3. **Review (R key):**
       - Verify two-column split: cluster list left, detail right
       - Navigate clusters with arrow keys -- detail panel should update
       - Select clusters with Space, then press A to approve
       - Check that a propagation toast appears (if applicable senders exist)
       - Press I to toggle inspect mode -- individual emails should appear in detail

    4. **Execute (E key):**
       - Verify approved items summary is shown
       - Click "Dry Run" to test -- progress bar should animate
       - Do NOT click "Execute for Real" unless you want to move emails

    5. **Pipeline (P key):**
       - Verify progress bar and log area are visible
       - (Only click "Run Pipeline" if you want to re-run scan/classify/analyze)

    6. **Theme (T key):** Toggle between dark and light -- verify readability

    7. **Help (? key):** Verify overlay opens with keybinding reference, Escape closes it

    8. **Quit (Q key):** Verify clean exit
  </how-to-verify>
  <verify>Human visual inspection of all TUI screens</verify>
  <done>User confirms TUI looks correct, all screens functional, keyboard navigation works, theme toggle works</done>
  <resume-signal>Type "approved" if the TUI looks and works correctly, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -q` -- full suite green
2. Human verification of visual appearance and interaction flow
3. All 4 screens accessible and functional
4. Help overlay and theme toggle working
</verification>

<success_criteria>
- All TUI screens render correctly and are navigable
- Help overlay provides keyboard reference
- Theme toggle works visually
- Human confirms the TUI meets expectations from CONTEXT.md decisions
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-interface-gui/04-04-SUMMARY.md`
</output>
