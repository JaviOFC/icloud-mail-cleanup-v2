---
phase: 04-interface-gui
task: UX-overhaul
total_tasks: 8
status: code_complete_pending_visual_verification
last_updated: 2026-03-07T08:58:46.341Z
---

<current_state>
TUI UX Overhaul is **code-complete** with all 376 tests passing (28 TUI tests, 2 new).
All 8 design-workshop consensus items are implemented. Need visual verification by running
`python -m icloud_cleanup tui` and checking the items in the verification checklist below.

Phase 4 original work (all 4 plans, 15/15 total) was already code-complete. This session
applied a UX overhaul based on a 4-perspective design conference audit.
</current_state>

<completed_work>

**Original Phase 4 (previous sessions):**
- Plan 04-01: TUI app shell, Dashboard screen, CLI wiring, test scaffold
- Plan 04-02: Review screen — cluster list/detail split, bulk actions, propagation, API fallback
- Plan 04-03: Execute & Pipeline screens — dry-run progress, background workers
- Plan 04-04: Help overlay, theme polish, E2E checkpoint + 7 UX fixes

**UX Overhaul (this session):**
- Step 0: Design conference report saved to `.planning/design-conference-report.md`
- Step 1: Navigation keys D/R/E/P → 1/2/3/4 in workflow order (1=Pipeline, 2=Dashboard, 3=Review, 4=Execute)
- Step 2: All overlay keybinding lists use Rich Tables (borderless, right-aligned key column) instead of manual spaces
- Step 3: Overlays widened from fixed `width: 64` to `width: 60%` (min 60, max 100); HelpScreen to `width: 70%`
- Step 4: New `ScreenHintBar` widget on all 4 screens (docked top, 1-line, shows available keys)
- Step 5: `h` key opens recallable screen-specific help (not just first visit); removed toast stacking from Review
- Step 6: Review detail panel enriched — structured sections (Overview/Top Senders/Example Subjects) + empty-state hints
- Step 7: Renamed jargon — "Triage" → "Auto-Sort", "Propagation" → "Similar Senders"
- Step 8: Removed dead `PlaceholderScreen` class
</completed_work>

<remaining_work>

- Visual verification: Launch TUI and verify all 12 checklist items below
- If all pass: commit all changes and close v1.0 milestone

### Verification checklist
1. Footer shows `1 Pipeline  2 Dashboard  3 Review  4 Execute` with active highlight
2. 1/2/3/4 keys switch screens correctly, default opens Dashboard
3. Welcome overlay: left-aligned prose, Rich Table for key descriptions, wider container
4. Each screen has persistent 1-line hint bar below Header
5. `h` key on any screen opens screen-specific help overlay (works every time)
6. `?` key opens full help overlay (wider, 1/2/3/4 keys, workflow order)
7. All overlay text uses Rich Tables for keybinding alignment (no manual spaces)
8. Review detail panel shows structured sections with actual sender addresses and subjects
9. Empty detail states show helpful hints instead of bare labels
10. No more stacking "Keybindings" toasts on Review screen
11. Dark/light theme toggle (T) still works with all new widgets
12. Review buttons read "Auto-Sort" and tab reads "Similar Senders"
</remaining_work>

<decisions_made>

- Kept dashboard skip in `show_screen_help_if_first_visit` — welcome overlay already covers dashboard orientation
- Used separate `recall_screen_help()` so `h` key always works regardless of visit state
- Overlay body text changed to `text-align: left` globally (was `center`) — multi-line content reads better left-aligned
- `ScreenHintBar` uses `dock: top` to sit right below Header, before screen content
</decisions_made>

<blockers>
None — all code changes complete, 376 tests passing.
</blockers>

<context>
UX overhaul driven by 4-perspective design conference (Interface Design, UI/UX Pro Max,
Graphic Designer, UX Heuristics). Full report: `.planning/design-conference-report.md`.
Targets Nielsen H6 (Recognition > Recall, was 3/10) and Krug Law 1 (Don't Make Me Think).
Expected scores after: heuristic 5.6→~8.5, graphic 4.8→~7.5.

Files changed: 14 existing + 1 new (`widgets/screen_hint.py`). 376 tests passing.
</context>

<next_action>
Run `python -m icloud_cleanup tui` with a real checkpoint and visually verify all 12 checklist items.
If all pass, commit and proceed to `/gsd:complete-milestone`.
</next_action>
